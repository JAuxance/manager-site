<section class="rail" id="features" aria-labelledby="rail-title">
  <div class="shell rail__inner">
    <p class="rail__label" id="rail-title" data-appear>
      <span data-lang="en">Built to keep your day moving</span>
      <span data-lang="fr">Pensée pour faire avancer ta journée</span>
    </p>

    <div class="rail__grid">
      <article data-appear style="--appear-delay: 0ms">
        <span class="rail__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="8" /><circle cx="12" cy="12" r="3.5" /><path d="M17.7 6.3 20 4" /></svg>
        </span>
        <h2><span data-lang="en">Stay focused</span><span data-lang="fr">Rester concentré</span></h2>
        <p><span data-lang="en">Timed sessions and reusable rhythms.</span><span data-lang="fr">Des sessions chronométrées et des rythmes réutilisables.</span></p>
      </article>

      <article data-appear style="--appear-delay: 90ms">
        <span class="rail__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24"><path d="m4 7 2 2 3-4M12 7h8M4 14l2 2 3-4M12 14h8M12 20h8" /></svg>
        </span>
        <h2><span data-lang="en">Set priorities</span><span data-lang="fr">Fixer les priorités</span></h2>
        <p><span data-lang="en">Today, tomorrow, recurring—always clear.</span><span data-lang="fr">Aujourd’hui, demain, récurrentes — toujours claires.</span></p>
      </article>

      <article data-appear style="--appear-delay: 180ms">
        <span class="rail__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24"><rect x="4" y="5.5" width="16" height="14" rx="2" /><path d="M8 3v5M16 3v5M4 10h16" /></svg>
        </span>
        <h2><span data-lang="en">Plan the week</span><span data-lang="fr">Planifier la semaine</span></h2>
        <p><span data-lang="en">Give each important block a real place.</span><span data-lang="fr">Donne une vraie place à chaque bloc important.</span></p>
      </article>

      <article data-appear style="--appear-delay: 270ms">
        <span class="rail__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24"><path d="M5 20V11M10 20V5M15 20v-7M20 20V8" /></svg>
        </span>
        <h2><span data-lang="en">See progress</span><span data-lang="fr">Voir la progression</span></h2>
        <p><span data-lang="en">Focus time, completed tasks and trends.</span><span data-lang="fr">Temps de focus, tâches terminées et tendances.</span></p>
      </article>
    </div>
  </div>
</section>

<style>
  .rail { position: relative; z-index: 2; padding: 0 0 clamp(4rem, 7vw, 6rem); }
  .rail__inner { border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
  .rail__label { padding: 1.2rem 0; border-bottom: 1px solid var(--rule); color: var(--mute-soft); font-family: var(--font-mono); font-size: 0.63rem; font-weight: 750; letter-spacing: 0.18em; text-align: center; text-transform: uppercase; }
  .rail__grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); }
  .rail article { min-width: 0; padding: clamp(1.6rem, 3.5vw, 2.8rem) clamp(1rem, 2.4vw, 2rem); }
  .rail article + article { border-left: 1px solid var(--rule); }
  .rail__icon { display: grid; width: 2.85rem; aspect-ratio: 1; place-items: center; border: 1px solid var(--rule); border-radius: 0.85rem; background: linear-gradient(145deg, var(--surface-hover), transparent); color: var(--accent); box-shadow: var(--shadow-soft); transition: transform 420ms var(--ease-out), border-color var(--dur-fast) var(--ease-out), color var(--dur-fast) var(--ease-out); }
  .rail__icon svg { width: 1.35rem; fill: none; stroke: currentColor; stroke-linecap: round; stroke-linejoin: round; stroke-width: 1.55; }
  .rail article:hover .rail__icon { border-color: color-mix(in srgb, var(--accent) 45%, var(--rule)); color: var(--accent-hover); transform: translateY(-0.25rem) rotate(-2deg); }
  .rail h2 { margin-top: 1.35rem; font-size: 0.98rem; letter-spacing: -0.025em; }
  .rail p { max-width: 14rem; margin-top: 0.65rem; color: var(--mute); font-size: 0.75rem; line-height: 1.55; }

  @media (max-width: 820px) {
    .rail__grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .rail article:nth-child(3) { border-top: 1px solid var(--rule); border-left: 0; }
    .rail article:nth-child(4) { border-top: 1px solid var(--rule); }
  }

  @media (max-width: 460px) {
    .rail__grid { grid-template-columns: 1fr; }
    .rail article { display: grid; grid-template-columns: auto 1fr; column-gap: 1rem; align-items: center; padding-block: 1.2rem; }
    .rail article + article, .rail article:nth-child(3) { border-top: 1px solid var(--rule); border-left: 0; }
    .rail h2 { margin-top: 0; }
    .rail p { grid-column: 2; margin-top: 0.3rem; }
  }
</style>
